slam_toolbox:
  ros__parameters:

    # === REQUIRED PARAMETERS ===
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan_reliable

    # === GENERAL PARAMETERS ===
    use_scan_time: true
    use_sim_time: false
    
    # Increase tolerance to solve timing issues
    transform_publish_period: 0.05
    map_update_interval: 1.0
    tf_buffer_duration: 30.0 # Give a large buffer for transforms

